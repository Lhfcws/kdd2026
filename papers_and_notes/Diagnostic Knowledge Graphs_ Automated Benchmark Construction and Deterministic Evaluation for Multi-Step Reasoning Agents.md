# Diagnostic Knowledge Graphs: Automated Benchmark Construction and Deterministic Evaluation for Multi-Step Reasoning Agents

## 背景与 Idea
评估 LLM Agent 的多步诊断推理（客服工单、故障报告、临床记录）长期是开放难题。从已解决案例中抽取的原因标签存在极端"词汇爆炸"：仅单一症状就从 2,196 条工单中抽出 5,076 个不同 cause 字符串、92% 仅出现一次，导致 LLM-as-judge 评分存在 ±2–3pp 的轮间方差、且与专家一致性仅中等（Cohen's κ=0.60），无法支撑纵向漂移监测。本文提出核心论点：构建可复现的诊断 benchmark 与构建高效诊断 Agent 是同一个"对偶问题"，可用同一件产物——规范化知识结构与受约束假设空间——同时解决。作者据此实例化出 Diagnostic Knowledge Graphs（DKG，带频率先验的层次化原因树）。

## 核心方法
DKG 是一棵有根树 G=(V, E, q, a)：节点为症状/原因/根因，边表示因果分解，q 将每节点映射到经校验的 SQL 查询（命中即 TRUE），a 将叶子映射到处置动作。构建分五阶段流水线：Stage 1 用 LLM（temperature 0）从已解决工单抽取"症状→原因链→动作"；Stage 2 聚类症状、LLM 分组为诊断族并生成层次树；Stage 2b 用两遍 BERTopic 聚类 + LLM 合并把成千上万表面形式压缩为规范簇（5,076→73，覆盖 97%）；Stage 3 以"工单即测试用例"的测试驱动思路为每节点迭代生成并校验 SQL（30 条工单、准确率阈值 0.7、最多 7 轮）；Stage 4 用确定性遍历算法自顶向下执行所有节点 SQL、收集命中节点交由 LLM 一次性综合；Stage 5 用新工单做增量扩充。评估采用双模式：semantic-judge 模式（对照原始 gold 链、公平跨方法比较）与 deterministic exact-match 模式（对照规范节点 ID、零方差、用于生产监控）。

## Performance 概括
基准构建在 14,953 条物流工单上产出 284 个症状族，无需人工整理；最大族 5,076 条原始 cause 压缩到 73 个规范簇（70× 压缩、97% 覆盖），TOP 10 族平均 57× 压缩、99% 覆盖。专家校验规范化准确率达 92%（50 例中 46 正确），超过同一专家与 LLM judge 的 80% 一致率；规范化后评估确定性为零轮间方差（σ=0），而 LLM judge 在 M2 上有 ±2–3pp 方差。五条件消融（3 个有 SQL 覆盖族、共 265 条票）显示：DKG+Tree（仅原因菜单+频率先验、无 SQL）在 receiving 族达 66% action、qty 族 49%，分别贡献 +32pp 增益的 78% 与 +44pp 的 86%；DKG+SQL 在 receiving 族 action 73%，与 DKG+Tree 的置信区间重叠，预校验 SQL 仅方向性正向、在 n=100 下不显著（需要 n≈900 才有统计力）；最难的 transit 族所有方法仅 32–43% 且 CI 重叠，树结构增益收窄至 +5pp。

## 论文价值与不足
价值在于清晰论证并实证了"评估—Agent 对偶性"：同一套规范化词汇既把 benchmark 词汇爆炸压平到精确匹配，又作为推理期菜单把 Agent 约束在历史原因上，带来 +32–44pp 的 action-accuracy 提升，且给出零方差、可纵向监控的确定性评估与逐层错误定位能力，对工业客服/IT 故障/医疗分诊有通用意义。不足：Agent 评估仅覆盖 6 个族（565 条票），SQL 消融只在 3 族；专家校验样本小（规范化 n=50、judge n=20）；transit 等高频分散族增益骤降，说明方法依赖频率集中；8% 的抽取失败率与嵌入模型偏差带来规范化噪声；生产监控虽在前提（92% 忠实、σ=0）上验证，但未经真实部署时间线的长期验证。

## 工业界落地可行性评估
落地可行性高且面向真实运维场景。流水线可由"已解决工单 + 运营数据库"自动构建可复现 benchmark 与 SQL-grounded 诊断 scaffold，已在内部物流客服系统实证（14,953 条工单、284 族），可直接迁移到 IT 事件管理、医疗分诊、金融反欺诈、制造缺陷分析等"有解决轨迹+可验证数据"的域。确定性精确匹配模式适合生产环境对模型更新/API 变更做小漂移监测；逐层错误分解可定位 Agent 失效环节以指导改进。落地前提是企业需具备已解决案例文本与可查询运营库，且效果在原因长尾、频率分散的场景会明显下降，需结合频率集中度的边界判断来决定是否采用。
