# Adaptive Decentralized Multi-LLM Orchestration with Accelerated Consensus-Based SPSA

## 背景与 Idea
多 LLM 系统中的自适应路由难以可靠评估：不同路由策略会把同一任务发给不同模型，于是观察到的差异混杂了路由决策质量与模型生成、API 行为、judge 噪声的随机性。本文把"如何可复现地评估自适应在线路由策略"作为 agentic AI 评估问题，提出 replay-based 评估协议——固定任务流与每个"任务–agent"对的潜在结果，使所有策略在相同条件下比较；并在该框架下采用已有的分布式加速共识型 SPSA（A-SPSA）做控制器参数的在线零阶优化。

## 核心方法
系统由多个 LLM agent（E）与多个控制器（C）组成：控制器估计任务处理时间与可等待时间、定义内部截止时间，并按 score = wᵀf − κ₁·截止缺口 − κ₂·预算超支 选择 agent。评估协议先构造结果表 Y（仿真由固定随机种子的 agent profile 生成，真实模式则预收集并缓存每个任务发往各端点的响应、延迟、成本、judge 分），回放时策略按自身历史选 agent、取缓存结果作反馈，从而把策略/适应行为与随机 LLM 调用分离。A-SPSA 用于优化控制器预测参数 θ⁽ᵖ⁾（处理时间）与 θ⁽ʷ⁾（等待预算）：每轮用同时扰动向量 Δ 的两点零阶反馈估计梯度，叠加加速代理更新（surrogate center z、序列 α_τ/γ_τ）与基于邻居参数差异的共识校正项，定期在滚动窗口上更新；参数变化经"路由→反馈→窗口更新→A-SPSA 适应→下次路由"闭环影响路由打分与截止时间。

## Performance 概括
数值仿真（3 控制器、5 异构 agent、N=800、7 个 seed、每 5 个任务更新一次、滚动窗口 25）中，A-SPSA 在 Routing Objective、Deadline Hit Rate（DHR）中位数最高、Wait MSE 最低，其中 DHR 分离最明显；对比基线含 consensus-SPSA、KW、ZO-PGD、ZO-GT、PD-2pt、SP-GT。组件消融显示：无 surrogate 变体的中间 Wait MSE 更低却未转化为更优下游路由，固定加速系数同时损害操作指标与预测误差，印证"更低中间预测损失≠更好下游路由"。真实 LLM 端点（200 任务、两流 seed {11,42}、5 个 Groq 端点，API 延迟用受控样本替代）下，A-SPSA 的 Routing Objective 0.839、DHR 0.722、Wait MSE 0.162，均优于 ZO-GT/KW/PD-2pt 等，仅 SP-GT 明显落后；A-SPSA 初期有适应暂态，积累反馈后维持最高持续 DHR。

## 论文价值与不足
价值在于给出可复现的多 LLM 路由评估协议（共享执行缓存、固定潜在结果），把策略比较从随机噪声中剥离，并以 A-SPSA 在路由效用、截止命中、等待预测三者上取得最佳平衡，且贡献了"中间预测损失下降不等于下游路由更好"的反直觉评估洞见。不足：数值仿真依赖人工设计的 agent profile；真实实验仅 5 个 Groq 端点、2 条任务流、API 延迟用合成样本替代，未覆盖服务端拥塞/限流/失败；质量估计受 LLM judge 偏好影响；实验仅 3 控制器/5 agent，更大网络、更密通信图与异构模型池的可扩展性未研究；A-SPSA 共识项（式 4）假设同步通信，未含通信延迟项，物理分离部署时需延迟感知扩展。

## 工业界落地可行性评估
中等可行。replay 协议可直接用于路由策略的离线对比与回归测试，A-SPSA 适合作为在线路由控制器的参数优化器，且在真实生成内容上复现了数值优势；代码已开源（github.com/newpotatato/decentralized-llm-spsa）。但真实验证规模小、延迟为合成、且仅限同址低通信开销场景，工业落地前需在目标生产环境做更大规模、含真实延迟与跨供应商网络的验证，并为分布式部署补充延迟感知的共识项；此外 judge 偏差与任务域覆盖有限也需在落地时加以校准与扩展。
