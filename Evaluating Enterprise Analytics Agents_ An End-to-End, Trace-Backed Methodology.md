# Evaluating Enterprise Analytics Agents: An End-to-End, Trace-Backed Methodology

## 背景与 Idea
企业分析 agent 产出跨多步、多工具的自然语言分析与图表，传统"最终答案是否正确"的评估不足以诊断失败根因。作者提出一个端到端、trace-backed 的方法论，把分析 agent 的评估覆盖为语义理解、执行质量、可靠性三类信号族，强调保留并回溯完整的执行轨迹（trace）以定位失败发生在哪一步。

## 核心方法
方法论用三组信号族评估：(1) 语义理解——问题是否被正确解析与理解；(2) 执行质量——工具调用、查询、中间计算是否正确；(3) 可靠性——跨运行一致性、边界与异常输入处理。核心机制是 trace-backed：保留每次工具调用与中间结果，使失败可归因到具体阶段（语义/执行/可靠性）而非仅看最终答案。案例研究用 50 个问题 × 2 种配置（如 baseline vs reasoning）× 3 次重复 = 300 条 traces 评估，并抽取 Finance 子集（30 traces）做契约检查（contract checks）。

## Performance 概括
Reasoning 配置将 early refusal（过早拒绝）从 73% 降至 0%，real-data 使用率从 21% 升至 73%；但 16% 的 trace 触及 tool-round 上限（执行超步），且 41/50 个问题在不同运行间改变了表格解释（跨运行不一致）。Finance 子集 30 条 trace 中，27/30 至少违反一项 contract check，显示可靠性问题显著。

## 论文价值与不足
价值在于提供一个可操作的端到端评估框架与 trace 回溯机制，使企业能定位 agent 失败根因（语义/执行/可靠性）而非仅看最终答案，并用真实企业分析场景验证。不足在于案例研究规模中等（50 问题），结论的跨域泛化需更多验证；contract check 由作者定义，标准化与可复用性不足；跨运行不一致（41/50）提示评估本身需多次重复以得到稳定结论。

## 工业界落地可行性评估
直接面向企业分析 agent 的生产评估需求（可靠性、契约合规、失败归因），方法论可嵌入企业 LLMOps / 评估管线作为上线前与回归测试组件，落地可行性高。
